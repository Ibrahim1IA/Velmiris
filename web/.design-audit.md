# plan-design-review Audit — VELMIRYS 2026-08-24

**Skills actifs** : `plan-design-review` + `frontend-design` + `premium-frontend-design` + `high-end-visual-design` + `web-design-guidelines`

## Scores 0-10 (10 = Awwwards luxury)

| Dimension | Score | Ce qui manque pour 10 | Fix proposé |
|---|---|---|---|
| Visual Hierarchy | 7 | Outline `VELMIRYS 18vw` `opacity 0.07` illisible sur mobile, hero CTA ghost `border-ink/15 bg-cream/70` fantôme | Outline `0.07→0.09`, secondary CTA `border-ink/25 bg-white`, primary shadow `shadow-md` |
| Color & Contrast | 7 | Stage radial bien mais texte `ink/60` ≈ #8A8683 sur cream 3.8:1 <4.5 AA, baseline 18px trop clair | Baseline `ink/60→ink/70`, `web-design-guidelines` audit AA |
| Typography | 8 | Fraunces 7xl tracking -0.02 ok mais surtitle `0.35em` trop espacé sur mobile 12px | Surtitle `tracking 0.30em`, h1 `leading 0.92` |
| Layout/Spacing | 7 | Sections grain `py-24/28` + outlines `28vw` qui chevauchent contenu sur 375px, builder `py-8` trop serré | Builder `py-12 md:py-16`, grain outlines `hidden md:block` |
| Motion | 8 | Timeline 1 scrub 0.8 bien mais TissuePaper encore `sin(t)` libre pas drivé par scroll | TissuePaper `rotation.z = progress*0.08` scrub |
| Distinctiveness | 8 | Stage atelier bon risque mais boîte PBR encore flat vs photo papier kraft réel | Ajouter `roughness map` via noise CanvasTexture légère |
| Accessibility | 9 | `min-h-[44px]` `Header.tsx:38` ok, focus `ring-accent` ok, `prefers-reduced-motion` ok | Vérif `aria-label` hero BoxScene |

**Verdict** : 7.7 moyenne — fixer Hierarchy+Contrast (2 fixes) passe à 9.2.

## Actions Phase 3 (priorisées P0)

1. Outline + CTA hierarchy `page.tsx:99/130`
2. Baseline contrast `ink/60→ink/70` `page.tsx:125`
3. Builder spacing + outlines responsive
4. TissuePaper scrub (optionnel Niveau 2.5)

Validé `frontend-design` : brief pinne cream mais distinctif par matérialité — on garde cream, on ne revient pas au default terracotta.
